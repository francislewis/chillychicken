Serial:
gcc  -g -o fft_serial.o fft_serial.c $(gsl-config --cflags) $(gsl-config --libs) -lm

OpenMP:
gcc fft_c_openmp_v1.c -o fft_c_openmp_v1 -fopenmp -O3

For all, include -g at the start for debugging with gdb
